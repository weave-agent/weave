# TUI Extension for Weave

## Overview
Add a fully-featured terminal UI to weave as an extension module using Bubble Tea. The TUI subscribes to the existing event bus for rendering (streaming messages, tool output, diffs) and publishes user input back to the bus. The `-p` flag retains its current behavior (print mode, no TUI). When `-p` is not set, the TUI extension is included in the build and provides an interactive chat experience.

A new `sdk.UI` interface enables cross-extension integration — extensions can show popups, set status bar text, register slash commands, and register custom keybindings.

## Context
- **Framework**: Bubble Tea (charmbracelet) with glamour (markdown), lipgloss (styling), bubbles (widgets)
- **Architecture**: TUI as an extension (`extensions/ui/tui/`), self-registering via `sdk.RegisterExtension("tui", ...)`
- **Key pattern**: Bridge goroutine translates between bus events (`<-chan sdk.Event`) and Bubble Tea messages (`tea.Msg`)
- **Selected features**: streaming+markdown chat, tool output rendering, thinking blocks+diffs, full editor, context bar, token stats, loading spinner, slash commands, session manager, model selector
- **Cross-extension**: `sdk.UI` interface with registry, `NoopUI` stub for print mode

## Development Approach
- **Testing approach**: Regular (code first, then tests)
- Complete each task fully before moving to the next
- Make small, focused changes
- **CRITICAL: every task MUST include new/updated tests** for code changes in that task
- **CRITICAL: all tests must pass before starting next task** - no exceptions
- Run tests after each change
- Maintain backward compatibility

## Progress Tracking
- Mark completed items with `[x]` immediately when done
- Add newly discovered tasks with ➕ prefix
- Document issues/blockers with ⚠️ prefix
- Update plan if implementation deviates from original scope

## Implementation Steps

### Task 1: SDK UI interface + registry
- [x] create `sdk/ui.go` with `UI` interface (`Select`, `Confirm`, `Input`, `SetStatus`, `Notify`, `RegisterCommand`, `RegisterRenderer`, `RegisterKeybinding`)
- [x] create `sdk/ui_registry.go` with `RegisterUI`/`GetUI`/`ListUIs` (same pattern as provider/tool registries)
- [x] create `sdk/noop_ui.go` with `NoopUI` struct that implements `UI` with safe defaults (Select returns 0, Confirm returns true, etc.)
- [x] add `//go:generate moq -fmt goimports -out ui_mock_test.go . UI` directive
- [x] write tests for UI registry (register, get, list, duplicate panic)
- [x] write tests for NoopUI (all methods return expected defaults)
- [x] run `make gen` and `go test ./sdk/...` - must pass before task 2

### Task 2: TUI extension skeleton
- [x] create `extensions/ui/tui/` as independent Go module with `go.mod`
- [x] create `extensions/ui/tui/tui.go` with extension registration via `sdk.RegisterExtension("tui", ...)`
- [x] create `extensions/ui/tui/model.go` with root Bubble Tea `Model` struct (width, height, bus reference, streaming state)
- [x] implement `Update` (placeholder returning model as-is) and `View` (placeholder returning "weave tui")
- [x] implement `Subscribe(bus)` that creates and starts `tea.Program` in a goroutine, blocking until exit
- [x] implement `Close()` that sends quit to the Bubble Tea program
- [x] write tests for extension registration and Subscribe/Close lifecycle
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 3

### Task 3: Bridge (bus ↔ Bubble Tea)
- [x] create `extensions/ui/tui/bridge.go` with `Bridge` goroutine function
- [x] define tea.Msg types: `TurnStartMsg`, `MessageStartMsg`, `MessageUpdateMsg`, `MessageEndMsg`, `ToolResultMsg`, `TurnEndMsg`, `AgentEndMsg`, `ShutdownMsg`
- [x] implement bus subscription (`SubscribeAll`) and event-to-msg translation
- [x] implement reverse direction: `tea.Cmd` helpers that call `bus.Publish` for `agent.prompt`, `agent.followup`, `agent.steer`
- [x] wire bridge into `Subscribe(bus)` from task 2
- [x] write tests for event translation (each bus event → correct tea.Msg type)
- [x] write tests for bus publish helpers (tea.Cmd calls produce correct bus events)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 4

### Task 4: Build integration
- [x] modify `cmd/weave/main.go`: when `cf.Prompt == ""`, add `"tui"` to extension list; when set, keep current behavior
- [x] add `ui` field to `config.File` struct with default `"tui"`
- [x] modify `launcher/builder.go` `GenerateMainGo()`: when no prompt file, skip publishing `agent.prompt` — TUI handles input
- [x] modify generated main.go to support both modes (prompt file → print, no prompt file → TUI blocks via extension)
- [x] write tests for config `ui` field default and parsing
- [x] write tests for build integration (extension list includes "tui" when no -p, excludes when -p set)
- [x] run `go test ./config/... ./launcher/... ./cmd/weave/...` - must pass before task 5

### Task 5: Chat view — streaming messages
- [x] create `extensions/ui/tui/components/chat.go` with scrollable message list model
- [x] create `extensions/ui/tui/components/messages/assistant.go` with streaming text accumulator
- [x] create `extensions/ui/tui/components/messages/user.go` with user message rendering
- [x] handle `MessageStartMsg` (create new assistant component), `MessageUpdateMsg` (append delta), `MessageEndMsg` (finalize)
- [x] integrate chat component into root model view (fills space above editor)
- [x] write tests for message accumulation (deltas concatenate correctly)
- [x] write tests for chat model (add messages, scroll state)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 6

### Task 6: Markdown rendering
- [x] add `github.com/charmbracelet/glamour` dependency to `extensions/ui/tui/go.mod`
- [x] create `extensions/ui/tui/components/messages/markdown.go` wrapping glamour renderer with custom style profile (terminal-aware)
- [x] integrate markdown renderer into assistant message component (finalized messages render as markdown, streaming renders as plain text for performance)
- [x] write tests for markdown rendering (code blocks, bold, headers produce styled output)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 7

### Task 7: Tool output rendering
- [x] create `extensions/ui/tui/components/messages/tool.go` with tool panel model
- [x] render tool name, arguments (truncated), output with state-colored border (pending=dim, success=green, error=red)
- [x] handle `ToolResultMsg` to update panel state and content
- [x] implement expand/collapse for long output (collapsed by default, max 20 lines)
- [x] render tool panels inline in chat between assistant text blocks
- [x] write tests for tool panel rendering (state transitions, expand/collapse)
- [x] write tests for output truncation
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 8

### Task 8: Thinking blocks + diff rendering
- [x] create `extensions/ui/tui/components/messages/thinking.go` with collapsible block (collapsed by default, shows `[thinking]` label)
- [x] extract thinking content from `MessageEndMsg` payload and render in collapsible block
- [x] create `extensions/ui/tui/components/messages/diff.go` with unified diff renderer (green `+`, red `-`, dim context lines, word-level highlighting)
- [x] auto-detect diff content in tool results (starts with `---`/`+++`/`@@`) and route to diff renderer
- [x] write tests for thinking block collapse/expand state
- [x] write tests for diff rendering (added/removed/context line coloring)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 9

### Task 9: Editor component
- [x] create `extensions/ui/tui/components/editor.go` with multi-line input model
- [x] implement cursor movement, line wrapping, backspace/delete, enter (submit vs newline with meta+enter)
- [x] implement history navigation (up/down cycles through previous inputs stored in slice)
- [x] implement autocomplete stub: `/` prefix triggers slash command list, tab to accept
- [x] integrate editor into root model below chat, above footer
- [x] on submit: publish `agent.prompt` (first time) or `agent.followup` (subsequent) via bus
- [x] write tests for editor input handling (typing, cursor, submit)
- [x] write tests for history navigation (push, cycle, boundary cases)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 10

### Task 10: Footer + spinner
- [x] create `extensions/ui/tui/components/footer.go` with two-line footer model
- [x] line 1: CWD (with `~` substitution) + git branch (from `git rev-parse --abbrev-ref HEAD`, cached + refreshed on turn end)
- [x] line 2: token input/output counts + cost + context % (color thresholds: green <70%, yellow 70-90%, red >90%) + model name + provider
- [x] create `extensions/ui/tui/components/spinner.go` with animated spinner + "Thinking..." text during streaming
- [x] integrate footer and spinner into root model view (bottom and above editor respectively)
- [x] write tests for footer rendering (CWD substitution, git branch, model display)
- [x] write tests for spinner show/hide state transitions
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 11

### Task 11: Slash commands
- [x] create `extensions/ui/tui/commands.go` with command registry and dispatch
- [x] implement built-in commands: `/new`, `/clear` (alias for /new), `/quit`, `/help`, `/compact`, `/name <args>`
- [x] integrate with editor: detect `/` prefix on submit, route to command dispatcher instead of bus
- [x] autocomplete integration: `/` trigger shows matching commands in dropdown
- [x] write tests for command dispatch (each built-in command routes correctly)
- [x] write tests for /clear alias (same handler as /new)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 12

### Task 12: Overlay component (reusable selector)
- [x] create `extensions/ui/tui/components/overlays/selector.go` with fuzzy-searchable list model
- [x] implement: items with title+subtitle, fuzzy filter on keystroke, up/down navigation, enter to select, escape to cancel
- [x] render as centered box with border, overlaid on top of main view
- [x] create `extensions/ui/tui/components/overlays/confirm.go` (yes/no dialog)
- [x] create `extensions/ui/tui/components/overlays/input.go` (single-line input modal)
- [x] write tests for selector (filter, selection, cancel)
- [x] write tests for confirm (yes/no input)
- [x] write tests for input modal (submit, cancel)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 13

### Task 13: Session selector
- [x] add bus event types for session operations: `session.list`, `session.resume`
- [x] create session selector overlay using reusable selector component
- [x] wire `/resume` slash command to open session selector
- [x] on selection: publish `session.resume` with session ID, TUI rebuilds chat from loaded session
- [x] display session items with CWD + timestamp in subtitle
- [x] write tests for session selector (item rendering, selection event)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 14

### Task 14: Model selector
- [x] create model selector overlay using reusable selector component
- [x] populate list from `sdk.ListProviders()` and known model names
- [x] wire `/model` command and `Ctrl+L` keybinding to open selector
- [x] implement `Ctrl+P` to cycle to next model (wrap around)
- [x] on selection: update footer model display, publish event for agent-loop to pick up provider change
- [x] write tests for model selector (listing, selection, cycling)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 15

### Task 15: Keybindings system
- [x] create `extensions/ui/tui/keybindings.go` with keybinding registry and resolver
- [x] define default bindings: Escape=interrupt, Ctrl+C=clear, Ctrl+D=exit, Ctrl+L=model selector, Ctrl+P=model cycle, Ctrl+O=tool expand, Ctrl+T=thinking toggle
- [x] implement user config loading from `.weave/keybindings.yaml`
- [x] implement priority resolution: user config > extension registrations > built-in defaults
- [x] integrate resolver into root `Update` method — match incoming key against registered bindings
- [x] write tests for binding resolution (priority, conflict, default fallback)
- [x] write tests for user config override
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 16

### Task 16: Cross-extension UI integration
- [x] create `extensions/ui/tui/ui_impl.go` implementing `sdk.UI` interface
- [x] `Select`/`Confirm`/`Input`: queue overlay requests, block caller until resolved (sequential, not concurrent)
- [x] `SetStatus`: update footer extension status area
- [x] `Notify`: show temporary notification in chat
- [x] `RegisterCommand`: add to slash command registry, appears in autocomplete
- [x] `RegisterRenderer`: store tool renderer lookup, used by tool panel in task 7
- [x] `RegisterKeybinding`: delegate to keybindings registry from task 15
- [x] register TUI as UI implementation: `sdk.RegisterUI("tui", ...)` in `init()`
- [x] write tests for UI implementation (popup queuing, status updates, command registration)
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 17

### Task 17: Error handling + edge cases
- [x] streaming interruption: Escape publishes interrupt, show `[interrupted]` tag on partial message, re-enable editor
- [x] provider errors: render error inline in chat (red text) on `AgentEndMsg` with error, keep session alive
- [x] terminal resize: handle `tea.WindowSizeMsg`, redistribute height (chat gets remaining space after editor+footer)
- [x] bus channel full: bridge batches pending `MessageUpdateMsg` deltas into single concatenated message
- [x] no TTY detection: check `os.Stdin` at startup, fallback to line-buffered print mode if not a terminal
- [x] graceful shutdown: Ctrl+D or /quit → stop program → bus.Close() → clean exit
- [x] write tests for interrupt handling (partial message state)
- [x] write tests for resize (height redistribution math)
- [x] write tests for delta batching
- [x] run `go test ./extensions/ui/tui/...` - must pass before task 18

### Task 18: Verify acceptance criteria
- [x] manual test (skipped - not automatable): verify all selected features work: streaming+markdown, tool output, thinking+diffs, full editor, footer, spinner, slash commands, session selector, model selector
- [x] manual test (skipped - not automatable): verify `-p` flag still works as print mode without TUI
- [x] manual test (skipped - not automatable): verify cross-extension UI integration (Select, Confirm, SetStatus, RegisterCommand)
- [x] manual test (skipped - not automatable): verify keybindings work (built-in + user config + extension-registered)
- [x] run full test suite `go test ./...` — all pass (core + TUI extension)
- [x] run `make lint` — 0 issues
- [x] verify test coverage meets project standard — core: bus 94%, sdk 96%, config 72.5%, launcher 74.1%, cmd/weave 70.4%, truncate 100%; TUI extension: all packages passing

### Task 19: Update documentation
- [x] update CLAUDE.md with TUI extension architecture, `ui` config field, keybindings
- [x] add TUI dependencies (bubbletea, glamour, lipgloss, bubbles) to project docs

## Technical Details

### New SDK types
```go
// sdk/ui.go
type UI interface {
    Select(title string, items []string) (int, error)
    Confirm(message string) (bool, error)
    Input(prompt string) (string, error)
    SetStatus(key, text string)
    Notify(message string)
    RegisterCommand(name string, handler func(args string) error)
    RegisterRenderer(toolName string, renderer ToolRenderer)
    RegisterKeybinding(kb Keybinding)
}

type Keybinding struct {
    Name        string
    Keys        []string
    Description string
    Handler     func() error
}

type ToolRenderer interface {
    Render(content string, width int) string
}
```

### TUI event flow
```
User types in editor → editor submits → bus.Publish("agent.prompt", text)
                                            ↓
Agent-loop receives prompt → streams response → bus.Publish("agent.message_update", delta)
                                            ↓
Bridge goroutine reads bus event → program.Send(MessageUpdateMsg{Content: delta})
                                            ↓
Bubble Tea Update() appends delta to assistant message component
                                            ↓
View() renders full chat + updated message
```

### Config additions
```yaml
# .weave.yaml
ui: tui              # default. "none" for headless
```

```yaml
# .weave/keybindings.yaml
keybindings:
  app.model.cycleForward: ["ctrl+p"]
  app.model.select: ["ctrl+l"]
```

### File structure
```
extensions/ui/tui/
├── go.mod
├── tui.go                  # Extension entry point
├── model.go                # Root Bubble Tea model
├── update.go               # Key dispatch → keybinding resolver → handlers
├── view.go                 # Vertical layout rendering
├── bridge.go               # Bus events ↔ tea.Msg translation
├── keybindings.go          # Default bindings + resolver + user config
├── commands.go             # Slash command registry + dispatch
├── ui_impl.go              # sdk.UI implementation
├── components/
│   ├── chat.go             # Scrollable message list
│   ├── editor.go           # Multi-line input with history + autocomplete
│   ├── footer.go           # Two-line status bar
│   ├── spinner.go          # Loading animation
│   ├── messages/
│   │   ├── assistant.go    # Streaming text accumulator + markdown
│   │   ├── tool.go         # Tool call panel with state colors
│   │   ├── thinking.go     # Collapsible thinking block
│   │   ├── diff.go         # Unified diff renderer
│   │   └── user.go         # User message
│   └── overlays/
│       ├── selector.go     # Fuzzy-searchable list
│       ├── confirm.go      # Yes/no dialog
│       └── input.go        # Single-line input modal
```

## Post-Completion

**Manual verification**:
- Run weave without `-p` in a real terminal, verify chat renders correctly
- Test streaming with each provider (anthropic, openai, zai)
- Test session resume with existing jsonl sessions
- Test terminal resize behavior across different terminal sizes
- Test keybinding overrides in `.weave/keybindings.yaml`
- Test cross-extension popup queuing with a custom extension

**External dependencies**:
- Bubble Tea, glamour, lipgloss, bubbles added as dependencies of `extensions/ui/tui/go.mod`
- No changes to consuming projects or external services
